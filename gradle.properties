# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
# Fabric Properties
minecraft_version=[VERSIONED]
loader_version=0.17.3
# Mod Properties
mod_version=0.3.5
maven_group=me.drex
archives_base_name=message-api
# Dependencies
placeholder_api_version=[VERSIONED]
# Testmod dependency
fabric_version=[VERSIONED]
adventure_version=[VERSIONED]